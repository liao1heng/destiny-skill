interface:
  display_name: "CLI PM"
  short_description: "Write markdown wireframe prototypes before any PM explanation docs"
  default_prompt: "Use $cli-pm to write or update Chinese PM docs only, and for page requests start with a `.md` wireframe prototype that shows the final page structure without explanation-heavy notes or source-code changes."
